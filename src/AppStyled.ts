import styled from "styled-components";

const AppStyled = styled.div`
  margin: 0;
  background: linear-gradient(
    0deg,
    rgba(0, 121, 255, 1) 0%,
    rgba(135, 45, 253, 0.7987570028011204) 74%
  );
  color: #2c3641;

  .main-container__title {
    margin: 0;
    padding: 20px;
    text-align: center;
    font-family: "DynaPuff", cursive;
    font-size: 2.5em;
  }
`;

export default AppStyled;
